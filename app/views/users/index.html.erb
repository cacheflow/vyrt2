<%=link_to "Can't see the users? Click here", users_path %> <%=link_to "Login", users_path %> 
<% if current_user %>
  <%= current_user.name %> 
<% else %> 
  <p>Hello Stranger You should sign in</p>
  <% end %> 
<div class="container">
  <div ng-controller="usersController" class="row">
    <h1>Users</h1>
    <br>

    Search: 
    <input 
     type="text" 
     search
     model="user" 
     update="users"
     ng-model="search">

    <table class="table table-striped table-bordered">
      <thead>
        <th> Name</th>
        <th>Email</th>
        <th>Age</th>
        <th>Location</th>
        <th>Created At</th>
         <th>Image</th>
      </thead>
      <tbody>
        <tr ng-repeat="user in users | filter:search" class="user">
          <td>{{user.name}}</td>
          <td>{{user.email}}</td>
           <td>{{user.age}}</td>
          <td>{{user.location}}</td>
          <td>{{user.created_at}}</td>
          <td>{{user.avatar}}
            <img ng-src='{{user.avatar}}'></img>
          </td>

        </tr>
      </tbody>
    </table>
    <br>
  </div>
</div>
